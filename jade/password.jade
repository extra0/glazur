extends _layout
block content
	.content
		.container-fluid
			.sidebar
				ul.breadcrumbs__list.list-inline
					li.breadcrumbs__item
						a.breadcrumbs__link(href="#") Главная
					li.breadcrumbs__item
						p.breadcrumbs__active Личный кабинет
				ul.sidebar__menu
					li.sidebar__menu-item
						a.sidebar__menu-link(href="#") Личный счет
					li.sidebar__menu-item
						a.sidebar__menu-link(href="#") история заказов
					li.sidebar__menu-item
						a.sidebar__menu-link(href="#") данные профиля
					li.sidebar__menu-item
						a.sidebar__menu-link(href="#") желания
					li.sidebar__menu-item
						a.sidebar__menu-link.active(href="#") смена пароля
			.main__section
				h1.main__title Смена пароля
				form.change-pass__form(action="#" method="post")
					input.modal__input(type="password" placeholder="Старый пароль" required)
					input.modal__input(type="password" placeholder="Новый парооль" required)
					input.modal__input(type="password" placeholder="Повторите пароль" required)
					button.button._to-cart._cart(type="submit") Сохранить
			.clear
			